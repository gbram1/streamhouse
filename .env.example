# StreamHouse Environment Configuration

# =============================================================================
# METADATA STORE
# =============================================================================

# SQLite (default for local development)
# DATABASE_URL=sqlite:./data/metadata.db

# PostgreSQL (Phase 3.2 - Production)
DATABASE_URL=postgres://streamhouse:streamhouse_dev@localhost:5432/streamhouse_metadata

# =============================================================================
# S3 / OBJECT STORAGE
# =============================================================================

# Use local MinIO (started with docker-compose)
USE_LOCAL_STORAGE=1
LOCAL_STORAGE_PATH=./data/storage

# Or use AWS S3
# STREAMHOUSE_BUCKET=my-streamhouse-bucket
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-key
# AWS_SECRET_ACCESS_KEY=your-secret

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Server listen address
STREAMHOUSE_ADDR=0.0.0.0:9090

# Cache directory and size
STREAMHOUSE_CACHE=./data/cache
STREAMHOUSE_CACHE_SIZE=1073741824  # 1GB

# Writer pool configuration (Phase 2.1)
FLUSH_INTERVAL_SECS=5

# =============================================================================
# LOGGING
# =============================================================================

# Log level (trace, debug, info, warn, error)
RUST_LOG=info

# Detailed logging for specific modules
# RUST_LOG=streamhouse_storage::writer_pool=debug,streamhouse_metadata=debug
